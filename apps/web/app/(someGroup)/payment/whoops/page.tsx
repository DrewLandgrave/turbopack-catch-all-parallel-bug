export default async function WhoopsPage() {
    // This is a server component, so you can fetch data directly here
    // const data = await fetchData();
    
    return (
        <div>
        <h1>Whoops</h1>
        <p>Something went wrong</p>
        </div>
    );
}  